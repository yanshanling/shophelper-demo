# 迭代记录

## v2.0（2026-09-17）· 飞轮闭环

### 背景

v1.0 解决了「配置硬编码、运营改不动」的问题，但基于使用流程框架（准备期资产 → 运行期对话 → 迭代期数据飞轮）自检后，发现飞轮存在 4 处断链（P0）：

1. **看不见线上对话**：没有对话日志，误判发生也无从知晓
2. **坏例回不去资产层**：发现问题只能口头记录，无法结构化回流到知识库 / 关键词 / Prompt
3. **测不了回归**：改配置后无法自动验证是否引入退化
4. **意图硬编码**：意图关键词写死在引擎里，加一个关键词要改代码发版

### 新增 4 个模块（5 → 9）

| 模块 | 演示要点 |
|---|---|
| 🎯 意图管理 | 意图关键词 / 权重 / 启停**配置化**，引擎实时读取 `DB.intents` 驱动打分，改关键词无需发版；内置意图级测试（各意图得分条形图） |
| 📋 对话日志 | 每次测试台对话自动写入会话记录（意图 / 置信度 / 安全等级 / 知识库命中 / Trace 回放）；支持按意图 / 标注状态筛选，4 项飞轮指标卡 |
| 🏷️ 标注回流 | 待标注队列 → 三分类标注（意图误判→补关键词 / 知识未命中→新建条目 / 回复差→关联 Prompt），一键回流资产层 |
| 🧪 测评集管理 | 分层用例（意图层 8 / 安全红线 3 / 端到端 2），**一键回归**全量跑测防止改配置引入退化，支持用例增删 |

### 引擎改造

- `recognizeIntent` 从硬编码关键词改为遍历 `DB.intents` 配置驱动打分（score = 命中数 × 权重 + 知识库 FAQ 动态加分）
- `runAgent` 每次对话写入 sessions 表，成为飞轮数据入口
- 数据版本升级 `LS_KEY` v1 → v2（自动重置演示数据）

### 闭环自检与修复

上线前用 29 项全链路用例做回归，发现并修复 3 个真实引擎缺陷：

- SHIPPING 缺 `where is my package` / `tracking number` 关键词，与 FAQ 打平误判
- COMPLAINT 缺 `删除我的账户`（中文用户偏好表达的账户注销诉求）
- 安全红线词库同步补齐 `删除我的账户`

最终 29/29 通过。

### 演示路径（v2.0 新增，约 3 分钟）

1. **意图管理** → 关闭 CHITCHAT 意图 → 回测试台发「你好」→ 识别为 FAQ（配置实时生效）
2. **对话日志** → 看刚才的会话已入库，点开详情回放 Trace
3. **标注回流** → 标注一条坏例「意图误判」→ 关键词自动补进意图配置
4. **测评集** → 一键回归 → 全绿（改配置后无退化）

---

## v1.0（2026-06 初）· 配置外置

- 单文件离线演示版：双击即开、零依赖，可发布 GitHub Pages
- 5 个模块：概览仪表盘 / 知识库管理 / Prompt 管理 / 大模型接入 / Agent 测试台
- 核心叙事：知识库、Prompt、模型配置全部外置，**改一条 FAQ 无需发版**
- Agent 引擎为后端 `agent_runtime.py` 的 1:1 JS 移植（意图加权打分 / 四级安全护栏 / ReAct Trace）
